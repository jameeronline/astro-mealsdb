export interface siteSettings {
    pageLength: number;
    // Add more common site settings as needed
    // e.g. theme: 'light' | 'dark';
    // language: string;
}

export const settings: siteSettings = {
    pageLength: 12,
    // theme: 'light',
    // language: 'en',
};